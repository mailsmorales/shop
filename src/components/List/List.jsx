import React from "react";
import "./List.scss";
import { Card } from "../Card/Card.jsx";

export const List = () => {
 

  return (
    <div className="list">
      {/* {data?.map((item) => (
        <Card item={item} key={item.id} />
      ))} */}
    </div>
  );
};
